import { Stack } from "expo-router";

export default function TeacherSanctionsLayout() {
  return (
    <Stack
      screenOptions={{
        headerTintColor: "#000000",
        headerTitleStyle: { fontWeight: "600" },
      }}
    />
  );
}
