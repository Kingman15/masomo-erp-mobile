import { Stack } from "expo-router";

export default function TeacherTeachingCoursesLayout() {
  return (
    <Stack
      screenOptions={{
        headerTintColor: "#000000",
        headerTitleStyle: { fontWeight: "600" },
      }}
    />
  );
}
